import { Story, Meta } from '@storybook/react/types-6-0';
import CardHeroPatchNotes from '.';

export default {
  title: 'CardHeroPatchNotes',
  component: CardHeroPatchNotes
} as Meta;

export const Default: Story = () => <CardHeroPatchNotes />;
